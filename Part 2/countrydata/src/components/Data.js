import React from "react"

const Data = ({ data }) => {
   return (
    <div>    
      {data.name} <br/>
    </div>
  )
} // A generalized module to print out the name component of an object.

export default Data